# Task Manager

企業・店舗向けの予約、担当者アサイン、対応状況管理を行うWebアプリです。

美容室、クリニック、自動車整備、法律・会計相談など、時間枠を使ってサービスを提供する複数業種に対応します。顧客自身によるWeb予約と、スタッフによる電話・店頭予約の登録に対応し、予約状況をカレンダーとカンバンで管理します。

Phase 1のアプリケーション基盤を実装済みです。確定した仕様と詳細設計は [docs/design.md](docs/design.md) を参照してください。

## MVP

- 1企業による複数店舗の管理
- 店舗スタッフの招待、所属、権限管理
- 公開予約ページとスタッフによる代理登録
- サービスメニュー、所要時間、料金目安の管理
- 店舗営業時間、休業日、スタッフ勤務時間の管理
- スタッフが対応可能なサービスの設定
- 設備・部屋・作業ベイなどの共有リソース管理
- 空き枠検索と二重予約防止
- 担当者指名、手動アサイン、自動アサイン
- 未アサイン予約の管理
- 日・週カレンダーと対応状況カンバン
- 顧客台帳と予約履歴
- 予約確認・変更・キャンセルメール
- 操作履歴

決済、SMS、Googleカレンダー連携、回数券、電子カルテ・診療記録などの業種固有機能はMVPの対象外です。

## 技術構成

- Next.js（App Router、TypeScript）
- React
- Prisma ORM
- PostgreSQL（Vercel Marketplace経由のPrisma Postgres）
- Auth.js（スタッフ認証・セッション管理）
- Argon2id（パスワードハッシュ）
- Resend（認証・招待・予約メール）
- Tailwind CSS
- dnd-kit（カンバンのドラッグ＆ドロップ）
- Zod（入力検証）
- Vitest / React Testing Library / Playwright
- Vercel（ホスティング、Preview、Production）

依存パッケージのバージョンは、実装開始時の安定版を固定して使用します。

## 開発ステータス

1. [x] 要件・基本設計
2. [x] Next.jsプロジェクト初期化
3. [ ] DB・スタッフ認証基盤（ログイン・初期企業登録を実装済み）
4. [ ] 企業・店舗・スタッフ・サービス設定（基本管理画面を実装済み）
5. [ ] 予約枠計算・予約管理
6. [ ] 公開予約ページ・メール
7. [ ] カレンダー・カンバン・自動アサイン
8. [ ] テスト・Vercelデプロイ

## ローカル開発

### 必要な環境

- Node.js 22.12以上
- npm 11以上
- PostgreSQL 17（このローカル環境では `192.168.100.2` のLinux上のDockerを利用）

### セットアップ

```bash
npm install
cp .env.example .env
npm run db:migrate
npm run dev
```

このローカル環境の `.env` は、Linuxホスト上のDockerで動作する `192.168.100.2:5432` のPostgreSQLへ接続します。Linux側で同梱の `compose.yaml` 相当のPostgreSQLを起動し、LAN内から5432番ポートへ接続できるようにしてください。アプリと同じマシンでDBを動かす場合のみ `docker compose up -d` を使用し、接続先を `localhost` に変更します。

`http://localhost:3000` でランディングページ、`http://localhost:3000/app` で管理画面の現在の実装を確認できます。

### 品質チェック

```bash
npm run lint
npm run typecheck
npm test
npm run build
npm run test:e2e
```

### Prisma

```bash
npm run db:generate
npm run db:migrate
npm run db:studio
```

Prisma 7の接続設定は `prisma.config.ts`、データモデルは `prisma/schema.prisma` にあります。アプリ実行時は `DATABASE_URL`、migration実行時は `DIRECT_URL` を使用します。

Vercelでは `vercel-build` により、デプロイ時に `prisma migrate deploy` を実行してからNext.jsをビルドします。

## 現在の実装

- Next.js 16 App Router / React 19 / TypeScript
- Tailwind CSS 4
- Prisma 7による予約・店舗・顧客・スタッフ・設備のデータモデル
- Prisma Postgres向けの接続基盤
- レスポンシブなランディングページと管理ダッシュボード
- テナント・店舗アクセスの認可ロジック
- Auth.js Credentialsによるスタッフログイン・ログアウトと管理画面保護
- Argon2idによるパスワードハッシュ
- 初期OWNER・企業の同時登録
- OWNER / ADMIN向け企業設定と店舗の作成・編集
- スタッフ招待・承認・取消、企業権限と所属店舗の管理
- 最終OWNERの権限変更・停止を防ぐ整合性チェック
- 1時間・一度限りのトークンによるパスワード再設定
- パスワード変更時の既存ログインセッション一括失効
- Resendを利用した認証メール送信基盤と送信履歴
- 24時間・一度限りのトークンによるメールアドレス確認
- サービスメニューの作成・編集・公開・停止管理
- 店舗ごとの曜日別営業時間（1日2枠・日跨ぎ対応）
- 休業日・臨時営業時間の登録と更新
- スタッフごとの所属店舗・対応可能サービス管理
- スタッフの曜日別勤務時間と休憩・休暇管理
- 設備種別・店舗設備・サービスごとの必要設備数管理
- 営業時間・勤務・休暇・既存予約・設備を統合した予約枠計算
- 公開空き枠APIと公開予約ページ
- 予約確定時の再検証とSerializableトランザクション
- PostgreSQL排他制約によるスタッフ・設備の二重予約防止
- 管理トークン付き予約確認画面
- 権限・所属店舗を反映した管理画面の予約一覧・詳細
- 楽観ロック付き予約ステータス更新と変更履歴
- 顧客用URLからの受付期限付きキャンセル
- キャンセル・受付不可時のスタッフ・設備枠解放
- 管理画面からの電話・店頭・スタッフ代理予約
- 顧客台帳の検索、予約履歴、業務メモ管理
- 予約自身を空き枠計算から除外した安全な予約日時変更
- 日時変更時のスタッフ・設備再割当と楽観ロック
- 担当割当一覧と勤務・休暇・スキルを再検証する手動担当変更
- 実データによる日・週カレンダーと店舗絞り込み
- 実データによる対応状況ボードとクイックステータス更新
- 予約受付・確定・受付不可・キャンセル通知メール
- OWNER／ADMIN向け操作履歴検索画面
- 顧客用URLからの受付期限付き予約日時変更
- 顧客変更時のスタッフ・設備再確保と通知メール
- 実データによる本日のダッシュボード集計
- DB接続と必須環境変数を確認するヘルスチェック
- メール送信履歴の管理画面
- Playwright E2EテストとGitHub Actions CI
- 企業・店舗更新の監査ログ
- 予約ステータス遷移
- 予約入力のサーバー向けバリデーション
- Vitestによる単体テスト

実環境でのマイグレーション・E2E実行、Vercel環境変数設定とデプロイが次の工程です。

初回は `/signup` で企業とOWNERを登録します。登録後は `/login` から管理画面へログインできます。企業・店舗設定はそれぞれ `/app/settings/organization`、`/app/settings/locations` で管理します。

スタッフ管理は `/app/settings/staff` で行います。メール送信連携前は、招待作成後に一度だけ表示されるURLを対象者へ共有してください。招待URLは7日間有効です。

## 環境変数

`.env.example` を参照してください。本番の秘密値をGitへコミットしないでください。

本番では `DATABASE_URL`、32文字以上の `AUTH_SECRET` と `TOKEN_HASH_SECRET`、公開URLを示す `APP_URL` を必ず設定します。マイグレーションでは `DIRECT_URL` を優先し、未設定の場合は `DATABASE_URL` を使用します。接続先がプール接続と直接接続を分けて提供している場合は、`DATABASE_URL` にプール接続URL、`DIRECT_URL` に直接接続URLを設定してください。メール送信には `RESEND_API_KEY` と、Resendで認証済みドメインの `EMAIL_FROM` が必要です。`/api/health` は環境変数とDB接続を検査し、異常時にHTTP 503を返します。
